# これは例であり、実際のトークンは絶対にコミットしないこと。
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# パス（Docker内のパス。マウントと対応させる）
# WORK_SOURCE=/work/source
# WORK_OUTPUT=/work/output
# 注: WORK_OUTPUT 未指定時は WORK_SOURCE/output を使用

# 入力音声ファイル名（WORK_SOURCE 配下に置く）
INPUT_FILENAME=meeting_sample.m4a

# 話者数（未指定なら推定寄り）
NUM_SPEAKERS=2

# 辞書ファイルのパス（未指定なら辞書機能は使わない）
# DICT_PATH=/work/dict/glossary_confirmed.tsv

# 辞書機能はオプション。デフォルトは両方オフ（安定運用）
# USE_LAYER_A=0
# USE_LAYER_B=0
# LAYER_A_FALLBACK=1
# DEBUG_GLOSSARY_LOG=0

# Layer A 用トークンバジェット（USE_LAYER_A=1 のときのみ。デフォルト 100）
# GLOSSARY_TOKEN_BUDGET=100
